---
description: Map the existing patterns/anti-patterns of a codebase (architecture, JS, framework). Run at the start of a mission, on an unfamiliar project.
agent: pattern-scan
---

Scan the patterns of: $ARGUMENTS

If no scope is given above, scan the whole project from its root. Produce the
pattern map per your output format (Architecture / JavaScript / Framework + Top 3
things to know). Read-only — change nothing.
